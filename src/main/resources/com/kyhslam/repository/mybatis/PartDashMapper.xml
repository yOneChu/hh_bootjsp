<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kyhslam.repository.mybatis.PartDashMapper">

    <!--전체 삭제-->
    <delete id="deleteAll">
        delete from PARTDASHBOARD where BLOCKNO != ''
    </delete>

    <!--    <insert id="save" useGeneratedKeys="true" keyProperty="id">
            insert into item(item_name, price, quantity)
            values(#{itemName}, #{price}, #{quantity})
        </insert>-->

    <insert id="savePartDashboard" >
        INSERT INTO PARTDASHBOARD(PARTALL, PARTACTIVE, PARTINACTIVE, BATCHDATE)
        VALUES( #{partDto.partALL}, #{partDto.partActive}, #{partDto.partInactive}, #{partDto.batchDate} )
    </insert>

    <!--    <update id="update">
            update item
            set item_name=#{updateParam.itemName},
                price=#{updateParam.price},
                quantity=#{updateParam.quantity}
            where id = #{id}
        </update>-->

    <select id="findByBlockNo" resultType="BlockHistoryDTO">
        select A.blockNo, A.blockName, A.version, A.gc_product,
               A.partType, A.uom, A.block_opt,
               A.pick, A.pickName, A.qty, A.UOM, A.MODDATE,
               A.cmt, A.color
        from BLOCK_HISTORY A
        where A.blockNo = #{blockNo}
    </select>


    <!--
        <select id="findAll" resultType="Item">
            select id, item_name, price, quantity
            from item
            <where>
                <if test="itemName != null and itemName != ''">
                    and item_name like concat('%', #{itemName}, '%')
                </if>
                <if test="maxPrice != null">
                    and price &lt;= #{maxPrice}
                </if>
            </where>
        </select>-->
</mapper>